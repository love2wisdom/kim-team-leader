---
description: Feature and code implementation with intelligent context awareness
---

# Implement Command

**Purpose**: Intelligent feature implementation following existing patterns

**Auto-Activations**:
- Persona: frontend, backend, architect, security (context-dependent)
- MCP: Magic (UI components), Context7 (framework patterns), Sequential (complex logic)
- Flags: --validate, --plan (for complex features)

**Arguments**:
- `$ARGUMENTS` - Feature description or specification
- `--type <impl>` - component | api | service | feature | integration
- `--framework <name>` - Specific framework to use
- `--with-tests` - Include test implementation
- `--dry-run` - Show implementation plan without executing

**Implementation Workflow**:
1. 📖 **Pattern Discovery**: Identify existing code patterns and conventions
2. 🎯 **Requirement Analysis**: Break down feature into implementable tasks
3. 🏗️ **Architecture Design**: Design implementation approach
4. 💻 **Code Generation**: Implement following project patterns
5. 🧪 **Testing**: Create comprehensive tests
6. ✅ **Validation**: Verify implementation against requirements
7. 📝 **Documentation**: Update relevant documentation

**Code Generation Philosophy**:
- Context-aware following existing patterns
- Framework-aligned conventions
- Security and performance by default
- Comprehensive error handling

**Quality Standards**:
- Pattern consistency with codebase
- Test coverage ≥80% for new code
- Security validation for sensitive operations
- Performance within established budgets

**Examples**:
- `/implement "User authentication with JWT" --type feature --with-tests`
- `/implement "Dashboard analytics component" --type component --framework react`
- `/implement "REST API endpoint for user profile" --type api --dry-run`

Execute implementation following SuperClaude development principles and SOLID design patterns.
