# ============================================
# 김팀장 프로젝트 환경변수 설정
# ============================================
# 이 파일을 복사하여 .env.local로 저장한 후 실제 값을 입력하세요.
# cp .env.example .env.local
#
# 주의: .env.local 파일은 절대 Git에 커밋하지 마세요!
# ============================================

# ===========================================
# 1. DATABASE (필수)
# ===========================================
# Supabase PostgreSQL 연결 문자열
# 발급: https://app.supabase.com > Project > Settings > Database > Connection string
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# ===========================================
# 2. NEXTAUTH 인증 (필수)
# ===========================================
# 개발: http://localhost:3000, 운영: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# JWT 암호화 키 (터미널에서 생성: openssl rand -base64 32)
NEXTAUTH_SECRET="your-generated-secret-key-here"

# ===========================================
# 3. GOOGLE OAUTH (필수)
# ===========================================
# 발급: https://console.cloud.google.com > APIs & Services > Credentials
# Redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ===========================================
# 4. GITHUB OAUTH (권장)
# ===========================================
# 발급: https://github.com/settings/developers > OAuth Apps
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# ===========================================
# 5. KAKAO OAUTH (선택 - 한국 사용자용)
# ===========================================
# 발급: https://developers.kakao.com > 내 애플리케이션
# Redirect URI: http://localhost:3000/api/auth/callback/kakao
# KAKAO_CLIENT_ID="your-kakao-rest-api-key"
# KAKAO_CLIENT_SECRET="your-kakao-client-secret"

# ===========================================
# 6. GOOGLE AI - GEMINI (필수)
# ===========================================
# 발급: https://aistudio.google.com/app/apikey
# 무료 tier: 분당 15요청, 일일 1,500요청
GOOGLE_AI_API_KEY="your-google-ai-api-key"

# 사용할 모델 설정
GOOGLE_AI_MODEL_DEFAULT="gemini-1.5-flash"
GOOGLE_AI_MODEL_ADVANCED="gemini-1.5-pro"

# ===========================================
# 7. GOOGLE CLOUD (필수 - Vertex AI, Storage)
# ===========================================
# 발급: https://console.cloud.google.com
# 1. 프로젝트 생성/선택
# 2. Vertex AI API 활성화
# 3. 서비스 계정 생성 > JSON 키 다운로드
GOOGLE_CLOUD_PROJECT_ID="your-gcp-project-id"

# 서비스 계정 JSON 키 파일 경로 (프로젝트 루트 기준)
# 보안: 이 파일도 .gitignore에 추가하세요!
GOOGLE_APPLICATION_CREDENTIALS="./keys/gcp-service-account.json"

# ===========================================
# 8. GOOGLE CLOUD STORAGE (필수)
# ===========================================
# 버킷 생성: https://console.cloud.google.com/storage
# 리전: asia-northeast3 (서울) 추천
GCS_BUCKET="your-gcs-bucket-name"

# ===========================================
# 9. 이미지 생성 - VERTEX AI IMAGEN (필수)
# ===========================================
# Vertex AI API가 활성화되어 있어야 합니다
# 위의 GOOGLE_CLOUD_PROJECT_ID와 GOOGLE_APPLICATION_CREDENTIALS 사용
VERTEX_AI_LOCATION="asia-northeast3"

# ===========================================
# 10. 대안 서비스 (선택)
# ===========================================

# --- Anthropic Claude (LLM 대안) ---
# 발급: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key"

# --- Stability AI (이미지 생성 대안) ---
# 발급: https://platform.stability.ai/account/keys
# STABILITY_API_KEY="sk-your-stability-api-key"

# --- Replicate (다양한 AI 모델) ---
# 발급: https://replicate.com/account/api-tokens
# REPLICATE_API_TOKEN="your-replicate-token"

# --- AWS S3 (스토리지 대안) ---
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# AWS_S3_BUCKET="your-bucket-name"
# AWS_REGION="ap-northeast-2"

# --- Cloudflare R2 (스토리지 대안 - 저렴) ---
# R2_ACCESS_KEY_ID="your-r2-access-key"
# R2_SECRET_ACCESS_KEY="your-r2-secret-key"
# R2_BUCKET="your-r2-bucket"
# R2_ENDPOINT="https://your-account-id.r2.cloudflarestorage.com"

# ===========================================
# 11. 앱 설정
# ===========================================
# 개발 환경
NODE_ENV="development"

# 앱 URL (개발)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 앱 이름
NEXT_PUBLIC_APP_NAME="김팀장"

# ===========================================
# 12. 개발 도구 (선택)
# ===========================================
# Prisma 로깅
# PRISMA_LOG_LEVEL="query,info,warn,error"

# 디버그 모드
# DEBUG="true"
